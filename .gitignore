# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with 'go test -c'
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# IDE specific files
.idea/
.vscode/
*.swp
*.swo

# OS specific files
.DS_Store
.env
**/.terraform/
# Terraform provider binaries (具体的除外)
**/terraform-provider-*
**/.terraform/providers/
**/.terraform/modules/
**/.terraform/terraform.tfstate
.terraform.lock.hcl
deployments/terraform/environments/development/terraform.tfvars
# Terraform plan files (機密情報含有のため除外)
*.tfplan
tfplan*
# Terraform state files (既存)
*.tfstate
*.tfstate.*
